---
type: concept
source: [[raw/papers/面向SSE流式转发的背压与内存治理研究.md]]
description: "SSE每连接独立有界队列：满则暂停读上游/降速/丢弃，杜绝无界缓冲与全局共享；队列水位Q(t)为上游到达与下游消费速率的积分差，治理本质是在Qmax处反向切断上游供给。"
created_at: 2026-09-14 23:20:00
updated_at: 2026-09-14 23:20:00
tags: [sse, bounded_queue, memory_governance, backpressure]
---

# SSE有界队列

## 核心结论

- 生产者不能无脑推，需加带最大长度的**阻塞队列**；队列满时暂停向LLM拉取token（对话场景不直接丢弃，避免丢token），水位下降后恢复消费。
- **每个SSE连接独立队列，严禁全局共享**——防止一个慢用户拖垮所有连接。
- 禁止无界队列/无界缓冲、先拼完整结果再发、另起异步读上游直接塞队列。

## 队列水位

$Q(t)$为服务端某连接在时间段$[0,t]$内的待推送量，等于上游到达量与下游消费量的积分差：

$$Q(t)=\int_0^t\bigl[\lambda_{up}(\tau)-\lambda_{down}(\tau)\bigr]\,d\tau$$

有界队列即对$Q(t)$施加硬上限$Q_{max}$：当$Q(t)\rightarrow Q_{max}$时暂停上游读取，使得$\lambda_{up}\rightarrow 0$，从而$Q(t)$回落。背压治理的本质即"在$Q_{max}$处反向切断上游供给"。

## 分级治理

根据水位分级处置：

| 水位 | 处置策略 |
|------|----------|
| 低水位 | 全速推 |
| 高水位 | 通知上游降采样/降速 |
| 硬上限 | 对话场景暂停生成等待消费；非关键场景（日志、摘要）允许丢弃旧消息 |

## 相关页面
- [[SSE背压与内存治理总览]]：五层框架全局视角
- [[背压透传]]：第一层——同步转发透传TCP背压
- [[慢消费者处置]]：第三层——队列持续高水位的处置决策
- [[Node.js stream背压]]：Node.js `res.write()`与highWaterMark关系

## 参考来源
- [[raw/papers/面向SSE流式转发的背压与内存治理研究.md]]